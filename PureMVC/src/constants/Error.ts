// TypeScript file


namespace constants.error {

    /**
     * 皮膚還沒加載完成
     */
    export let SKIN_NOT_LOAD_YET: string = "皮膚還沒加載完成";
    /**
      * 沒有任何的 layer remove
      */
    export let EMPTY_LAYER: string = "沒有任何的 layer remove";
    /**
      * 想 remove 的 layer 數量不正確
      */
    export let UNEXPECT_LAYER: string = "想 remove 的 layer 數量不正確";
    /**
      * 超過預想所需要的 index 
      */
    export let ARRAY_UNEXPECTED_INDEX: string = "超過 array index";
    /**
      * 進入 switch default的 index
      */
    export let SWITCH_DEFAULT_VALUR: string = "進入 switch default的 index";
}

