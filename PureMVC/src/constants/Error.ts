// TypeScript file


namespace constants.error {

    /**
     * 皮膚還沒加載完成
     */
    export let SKIN_NOT_LOAD_YET: string = "皮膚還沒加載完成";
    /**
      * 沒有任何的 child subview
      */
    export let NOT_SUCH_LAYER: string = "沒有任何的 child subview";
    /**
      * 超過預想所需要的 index 
      */
    export let ARRAY_UNEXPECTED_INDEX: string = "超過預想所需要的 index";
    /**
      * 超過預想所需要的 index 
      */
    export let SWITCH_DEFAULT_VALUR: string = "超過預想所需要的 index";
}

